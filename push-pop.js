// as we've seen we can add a new value in a array with just using the name of the array but here we can do the same wit push as well but with only push we can add a new value at the end
var marks = [84, 99, 39, 33, 92];
marks.push("beautiful");
// console.log(marks);
// add a new value with push at the beginning of the array

marks.unshift("45");
console.log(marks);

// pop or remove th last element from the array
marks.pop();
// console.log(marks);
// remove the value at the beginning of the array  with push
// marks.shift();
console
